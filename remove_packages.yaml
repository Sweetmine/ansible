---

- hosts: all
  become: true
  tasks:
  - name: Remove packages for CentOS/RedHat
    dnf:
      name: 
        - tmux
        - httpd
      state: absent
    when: ansible_distribution in ["CentOS", "RedHat"]
