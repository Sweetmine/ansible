---

- hosts: all
  become: true
  tasks:
  - name: Remove packages for Each OS Distribution
    package:
      name: 
        - "{{ tmux_package }}"
        - "{{ apache_package }}"
      state: absent
